# Defaults for the desktop client. `signalingUrl` is kept in sync with
# reactionjs/deploy.properties by `node reactionjs/scripts/apply-constants.mjs`
# (when both repos are checked out side by side). `--signaling` on the command line overrides it.
signalingUrl=https://reaction.rimtimtagidim.workers.dev
